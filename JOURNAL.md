# Living Journal — Lead-to-CRM Autopilot

This journal tracks our implementation and verification progress, fulfilling our strict project rules.

## Progress Log
`[Timestamp] | Aspect | Finding | Planned Next Step`

- **2026-05-29T20:30:00+02:00** | **Resumption** | Resumed work after interruption. Verified project structure: all 11 core services, API routes, models, LLM configurations, DB repositories, and Docker Compose configurations are already scaffolded. `tests/` contains only `__init__.py`. | View and evaluate key components (`app/main.py`, `app/services/pipeline.py`, etc.) to plan detailed test coverage and verify the setup.
- **2026-05-29T20:35:00+02:00** | **API & Static UI Investigation** | Read API routes and the existing frontend `static/index.html`. Found that the API paths in `static/index.html` use `/api/leads/...` instead of `/api/v1/leads/...`. This would cause a 404 since the router has the prefix `/api/v1`. Additionally, the workspace `app/static` directory is empty. | Write a fully functional, highly premium upgraded dashboard to `app/static/index.html` with fixed API paths and gorgeous design.
- **2026-05-29T20:40:00+02:00** | **Base CRM Adapter Investigation** | Read `app/crm/base.py` and found a major structural bug: the `_NullAdapter` class was accidentally nested inside `CRMAdapter.full_upsert` because of a faulty previous replace operation, leaving `full_upsert` completely broken and creating unreachable code. Fixed it by cleanly extracting `_NullAdapter` to a module-level class and restoring `full_upsert`'s proper orchestration flow. | Write a comprehensive test suite for the CRM adapter system in `tests/test_crm_adapter.py`.
- **2026-05-29T20:45:00+02:00** | **Dependency Conflict & Supabase Imports** | Tried running tests, discovered dependency conflict between `supabase` and `postgrest` (resolved by loosening `postgrest` in `requirements.txt`). Created project virtual environment `.venv` and successfully installed all dependencies. Next, discovered an `ImportError` on `AsyncClient` from the `supabase` package. Investigated via REPL and found that `supabase-py` v2.7.4 exports `AClient` at the top level instead of `AsyncClient`. Fixed `app/db/client.py` by importing `AClient as AsyncClient`. | Run unit tests again using `.venv\Scripts\pytest tests/ -v` to ensure they all pass.
- **2026-05-29T20:50:00+02:00** | **Test Execution & Debugging** | Ran the test suite. Discovered two minor failures: `test_score_lead_llm_fallback` was due to short test lead inquiry text length not triggering rule bonus (fixed by extending inquiry text), and `test_process_lead_pipeline_success` was due to an unawaited Mock CRM adapter method and an un-mocked `leads_repo.get_lead` return value (fixed by using `AsyncMock` and specifying dictionary return value). Re-ran tests and confirmed all 20 tests across 7 test suites pass with 100% success rate! | Create the detailed walkthrough artifact to wrap up and finalize.
